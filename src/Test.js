function Test() {
  return (
    <div style={{
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      padding: "50px",
      color: "white"
    }}>
      <h1 style={{ fontSize: "40px", marginBottom: "20px" }}>🧪 Test</h1>
      <p style={{ fontSize: "18px", opacity: 0.8 }}>
        This is the Test page. You can use it to try new features.
      </p>
    </div>
  );
}

export default Test;
